apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: relentless-api
  namespace: default
  labels:
    app: relentless
spec:
  selector:
    matchLabels:
      app: relentless-api
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - path: /metrics
      port: http
